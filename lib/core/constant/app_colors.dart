import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const scaffoldBg = Color(0xFF111111); // main scaffold bg
  static const cardBg = Color(0xFF1C2529); // cards, bottom nav
  static const appBarBg = Color(0xFF263338); // app bar
  static const navBg = Color(0xFF3B565F); // statement, more, support
  static const loginBg = Color(0xFF314047); // login sections
  static const inputFill = Color(0xFF3E515A); // text field fill
  static const gridMoreBg = Color(0xFF394B53); // the "more" circle in grids

  // Text
  static const textPrimary = Colors.white;
  static const textSecondary = Colors.white70;
  static const textHint = Colors.white54;
  static const textDivider = Colors.white24;

  // CircleAvatar
  static const circleAvatar = Color.fromARGB(255, 57, 75, 83);

  //
  static const container = Color.fromARGB(255, 59, 86, 95);

  // Status
  static const success = Color(0xFF4CAF50); // green checkmarks, login button
  static const danger = Color(0xFFD61212); // red debit amounts

  // Icons
  static const iconMuted = Colors.white70;
}
